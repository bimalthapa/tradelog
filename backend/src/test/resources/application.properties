# Override ddl-auto for tests: SQLite reports INTEGER not BIGINT, so validate fails.
# Flyway still runs migrations and builds the schema; Hibernate just skips type-checking.
spring.jpa.hibernate.ddl-auto=none
